# Model Dependency Parser

Parsers vllm or sglang code and identifies the key dependencies of a model in the repository.

## Steps to use

```
python model_parser.py --root <Repo root directory> --format <text/json> <model file path>

```
